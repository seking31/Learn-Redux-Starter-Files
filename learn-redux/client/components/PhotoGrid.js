import React from 'react';
import { Link } from 'react-router';

function PhotoGrid(){

     return (  
     <div className="photo-grid">
        photo-grid
    </div>
        )
    
};

export default Single;